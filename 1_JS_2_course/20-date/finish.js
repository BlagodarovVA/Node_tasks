/** ЗАДАЧА 20 - Дата
 *
 * 1. Создайте переменную и присвойте ей текущее время в миллисекундах
 *
 * 2. Выведите значение в консоль
 */

const currDate = new Date();

const currDateInMs = currDate.getTime();

console.log(currDate);
console.log(currDateInMs);